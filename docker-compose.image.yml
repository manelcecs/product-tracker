services:
  product-tracker:
    image: product-tracker:v1
    container_name: product-tracker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
